{
  "name": "rstm.me",
  "version": "0.1.0",
  "description": "Personal site",
  "author": "Rustam Yusupov",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rustamyusupov/rustamyusupov.github.io.git"
  },
  "scripts": {
    "start": "parcel ./src/*.html",
    "build": "npm run clean && parcel build ./src/*.html --no-source-maps",
    "clean": "rm -rf dist/ .parcel-cache",
    "deploy": "npm run build && cp CNAME dist/ && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "parcel": "latest"
  }
}
